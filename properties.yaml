threadCount: 10

fromProperties:
  url: jdbc:oracle:thin:@(description=(address=(host=localhost)(protocol=tcp)(port=1522))(connect_data=(service_name=SOURCE)))
  user: ora2pg
  password: ora2pg
toProperties:
  url: jdbc:postgresql://localhost:5432/db1
  user: user1
  password: password
