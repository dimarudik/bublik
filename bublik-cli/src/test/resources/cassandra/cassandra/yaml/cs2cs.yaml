threadCount: 10

fromProperties:
  class: dev.bublik.cassandra.storage.CassandraStorage
  datacenter: datacenter1
  hosts: localhost
  keyspace: test
  user: cassandra
  password: cassandra
  batchSize: 256
toProperties:
  class: dev.bublik.cassandra.storage.CassandraStorage
  datacenter: datacenter1
  hosts: localhost:9043
  keyspace: test
  user: cassandra
  password: cassandra
  batchSize: 256
